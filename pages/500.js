export default function Custom500() {
  return (
    <h1
      style={{
        position: 'relative',
        margin: 'auto',
        background: 'var(--white)',
        color: 'var(--black)',
        padding: '1rem',
        borderRadius: '0.5rem',
      }}
    >
      {' '}
      500 | Server Error{' '}
    </h1>
  );
}
